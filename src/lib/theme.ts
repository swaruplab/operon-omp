// Theme is now defined inline in CodeEditor.tsx and registered via beforeMount.
// This file is kept for backward compatibility but is no longer used.
export function registerMonacoTheme() {
  // no-op — theme registration moved to CodeEditor beforeMount
}
